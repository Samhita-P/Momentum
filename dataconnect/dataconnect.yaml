specVersion: "v1beta"
serviceId: "momentum"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "momentum-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
